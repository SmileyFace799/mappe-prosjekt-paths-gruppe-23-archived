package no.ntnu.idata2001.g23.controllers;

import no.ntnu.idata2001.g23.view.PathsApp;

/**
 * A controller for the play game scene.
 */
public class PlayGameController extends GenericController {
    /**
     * Makes a play game controller.
     *
     * @param application The application itself
     */
    public PlayGameController(PathsApp application) {
        super(application);
    }
}
