package no.ntnu.idata2001.g23.entities;

/**
 * An enemy.
 * TODO: Implement this
 */
public abstract class Enemy extends GenericEntity {

    protected Enemy(int maxHealth, int inventorySize) {
        super(maxHealth, inventorySize);
    }
}
